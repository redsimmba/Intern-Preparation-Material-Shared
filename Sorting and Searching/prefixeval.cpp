#include<bits/stdc++.h>
using namespace std;
int evalprefix(string s){
	int n=s.length();
	stack<double> st;
	for(int i=n-1;i>=0;i--){
		if(s[i]==' '){
			continue;
		}
		if()
	}
}
int main(){
	string s;//String s is the input expression
	cin>>s;
	cout<<evalprefix(s);



	return 0;
}